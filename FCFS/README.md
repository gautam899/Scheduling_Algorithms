# FCFS: First Come First Serve

- FCFS is the simples CPU-scheduling algorithm. With this scheme the process that request the CPU first is allocated the CPU first.

- FCFS is a non-preemptive i.e. once the cpu is assigned to a process it is only freed once the process terminates and not before that.
