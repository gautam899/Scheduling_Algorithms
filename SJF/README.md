# Shorted Job First (SJF)

- In Shortest Job First(SJF) algorithm the process with the shortest cpu burst is assigned a CPU core first.

- If the length of CPU burst of two processes is same, FCFS is used to break the tie.